<!DOCTYPE html>
<html>
<head>
  <title>Job Details | GigVault</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    body {
      background-color: #f4f4f4;
      font-family: Arial, sans-serif;
    }

    .container {
      max-width: 700px;
      margin-top: 40px;
      background-color: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .job-title {
      font-size: 24px;
      font-weight: bold;
    }

    .job-meta {
      font-size: 14px;
      color: #666;
      margin-bottom: 15px;
    }

    .job-description {
      font-size: 16px;
      line-height: 1.6;
      margin-bottom: 30px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="job-title"><%= job.title %></h1>
    <div class="job-meta">
      Posted by @<%= job.owner_name %> | $<%= job.budget %> | <%= new Date(job.created_at).toDateString() %>
    </div>

    <div class="job-description">
      <%= job.description %>
    </div>

    <% if (typeof success !== 'undefined') { %>
  <div class="alert alert-success alert-dismissible fade show" role="alert">
    Application sent successfully!
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% } %>

    <form action="/dashboard/apply/<%= job.id %>" method="POST">
      <div class="mb-3">
        <label for="message" class="form-label">Why should we pick you?</label>
        <textarea class="form-control" id="message" name="message" rows="3" required></textarea>
      </div>
      <button type="submit" class="btn btn-primary">Submit Application</button>
    </form>
  </div>
</body>
</html>
