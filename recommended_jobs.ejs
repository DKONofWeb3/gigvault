<!DOCTYPE html>
<html>
<head>
  <title>Recommended Jobs | GigVault</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body { background-color: #f9f9f9; font-family: Arial, sans-serif; }
    .container { margin-top: 40px; }
    .card { box-shadow: 0 2px 8px rgba(0,0,0,0.05); margin-bottom: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <h3 class="mb-4">🧠 Recommended Jobs for <%= user.username %></h3>

    <% if (jobs.length === 0) { %>
      <div class="alert alert-info">No personalised recommendations yet. Apply to more jobs to improve your suggestions.</div>
    <% } %>

    <% jobs.forEach(job => { %>
      <div class="card p-3">
        <h5><%= job.title %></h5>
        <p><%= job.description.substring(0, 150) %>...</p>
        <p><strong>Budget:</strong> $<%= job.budget %> | <strong>Posted by:</strong> <%= job.owner_name %></p>
        <a href="/dashboard/job/<%= job.id %>" class="btn btn-primary btn-sm">View Job</a>
      </div>
    <% }) %>
  </div>
</body>
</html>
